# The start button (basicly-fiow1sr) and the module-size split it forced.
#
# `board_actions` crossed the 4000-token cap when the start action was added. The gate's
# remedy is a split along a nameable responsibility, and the module already had one: the
# table of verbs, and the endpoint that runs them. `board_action_surface` is the second half.
#
# That split raised `comment-density` on what remained, because the code left and the
# declarations stayed. The waiver is at column 0 in that module and names basicly-kya2os.1,
# which owns the question of whether a prose ratio measures a declaration table correctly.
[ratchet.comment_density]
count_delta = 1
