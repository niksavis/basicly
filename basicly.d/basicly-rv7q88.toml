# The consumer surfaces name the three ways to reach the install verb (basicly-rv7q88).
#
# ## spend_accuracy: one banked pair
#
# The lane spent 1,025,735 tokens against a recorded forecast of 11,610,753 - 0.088x, outside
# the 10x band - so its close commit was refused. The estimator, not the lane, produced that:
# a scope of 12,598 tokens over five documentation files, multiplied by the `task` build factor
# of 3.0, which is a declared seed that has never been measured. Banked as history with the
# defect named, per the gate's own remedy for a finished lane; basicly-yjmxjz owns the fix.
[ratchet.spend_accuracy]
count_delta = 1
