# `check_comment_density.py` now reads its baseline through `basicly.dropin.compose`, the
# way the other two ratchet gates already do, which is one more import that has to follow
# the `sys.path.insert` making it resolvable — the same `# noqa: E402` those two carry, for
# the same reason. It names it, so `unreasoned_count` does not move.
[ratchet.noqa_debt.frozen]
E402 = 1
