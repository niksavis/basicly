# yaml-language-server: $schema=../../schemas/fragment.schema.json
schema_version: 1
id: claude-defaults
description: Claude-specific defaults and file layout conventions.
category: project
priority: medium
applies_to:
- claude
tags:
- claude
- layout
status: active
title: Claude-specific notes
body: |
  - Author skills and rules under `.basicly/core/` (or the `.basicly-local/` overlay); `.claude/skills/` and `.claude/rules/*.md` are projected output — regenerate, never edit.
  - Put Claude-only prompts/commands/runbooks in `.claude/*.md`.
